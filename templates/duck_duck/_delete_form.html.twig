<form method="post" action="{{ path('duck_duck_delete', {'id': duck_duck.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ duck_duck.id) }}">
    <button class="btn btn-success">Delete</button>
</form>
