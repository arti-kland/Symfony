{% extends 'base.html.twig' %}

{% block title %}DuckDuck{% endblock %}

{% block body %}
    <h1>DuckDuck</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ duck_duck.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ duck_duck.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ duck_duck.lastname }}</td>
            </tr>
            <tr>
                <th>Duckname</th>
                <td>{{ duck_duck.duckname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ duck_duck.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ duck_duck.password }}</td>
            </tr>
        </tbody>
    </table>
    {% if is_granted('ROLE_ADMIN') %}
    <a class=" btn btn-success" href="{{ path('duck_duck_index') }}">back to list</a>
    {% endif %}
    <a class=" btn btn-success" href="{{ path('duck_duck_edit', {'id': duck_duck.id}) }}">edit</a>

    {{ include('duck_duck/_delete_form.html.twig') }}
{% endblock %}
